{
  "name": "n-chances-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply n-chances-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply n-chances-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
